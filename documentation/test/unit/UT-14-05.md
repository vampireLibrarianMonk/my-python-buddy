**Test ID:** UT-14-05
**Test Case Name:** Training Notebook Validation – Merged Model Artifacts Creation and Evaluation
**Test Type:** Unit
**Test Medium:**

- **File:** `notebook/Fine_Tuning_Model.ipynb`
  **Cell Series:** `20-22`

**Description:**

The following `Unit Test` is part five of a series that is helmed by a human so that the merged LoRA-enhanced LLaMA-2 model can be automatically loaded, combined, tokenized, archived, integrity-verified and then uploaded to Google Drive. The resulting merged model is then checked in an prompt generation test to ensure the model generates output.

**Preconditions:**

- Unit Test 14-04 is performed.

**Test Data:**

```python
# Define user question and conversation messages
question = (
    "How can I write a Python program that calculates the mean, standard "
    "deviation and coefficient of variation of a dataset from a CSV file?"
)
messages = [
    {"role": "system", "content": "You are a helpful Python tutor."},
    {"role": "user", "content": question},
]
```

**Execution Steps:**

1. Shift + Enter for prescribed cells.

**Expected Result:**

- Loaded checkpoints shards displayed.
- Merged model saved locally then uploaded to Google Drive, both paths are displayed.
- Merged model is hashed, hash displayed and hash file uploaded to Google Drive with its respective path displayed in Google Drive.

**Post-Conditions:**

- Merged model and hash reside in Google Drive.
- Prompt generation has successfully occurred.

**Associated Requirements:**
FR-14
