**Test ID:** UT-14-08
**Test Case Name:** Training Notebook Validation – Llama-cpp-python Installation and GPU Inference Check
**Test Type:** Unit
**Test Medium:**

- **File:** `notebook/Fine_Tuning_Model.ipynb`
  **Cell(s):** `30-32`

**Description:**

The following `Unit Test` is part eight of a series that is helmed by a human so that llama-cpp-python is installed and its inference capability is checked to ensure that it recognizes the Google Colab GPU (A100). The inference check is first the metadata print of the llama-cpp-python to check if it recognizes the GPU then there is an inference prompt check to validate that the GPU is actually utilized.

**Preconditions:**

- Unit Test 14-07 is performed.

**Test Data:**

````python
# Define assistant role and secure review instructions
system_prompt = (
    "You are a superb Python programmer well versed in secure coding and practices. "
    "Identify the vulnerabilities and provide concise, production-ready mitigations. "
    "You will utilize secure defaults and ensure minimal use of dependencies."
    "Use minimal code comments."
    "Refrain from providing payloads that exploit vulnerabilities or any step by step process to attack any software. "
    "When asked to rewrite code ensure you preserve functionality where possible."
)

# Vulnerable example demonstrating unsafe pickle deserialization
code_snippet = """import pickle

def load_user_data(serialized_data):
    # Deserializes user data from a byte stream.
    user_data = pickle.loads(serialized_data)
    return user_data

if __name__ == "__main__":
    malicious_input = input("Enter serialized data: ")
    try:
        serialized_data = bytes.fromhex(malicious_input)
        load_user_data(serialized_data)
    except Exception as e:
        print(f"Deserialization failed: {e}")"""

# Instruct LLM to analyze and securely rewrite code
user_prompt = (
    "Please review the following script and do all of the following in order:\n"
    "1) Identify the primary vulnerability and why it’s dangerous.\n"
    "2) Explain realistic impact and when it can be exploited.\n"
    "3) Rewrite the program securely while keeping the original functionality.\n"
    "4) Briefly list safer serialization options and when to use them.\n"
    f"Here is the code to analyze and refactor:\n\n```python\n{code_snippet}\n```"
)
````

**Execution Steps:**

1. Shift + Enter for prescribed cells.

**Expected Result:**

- `llama_cpp_python-0.3.16-cp312-cp312` python wheel is installed and exact same version is printed.
- llama-cpp info is printed with CUDA showing up and the GPU SM architecture listed for the A100 (800).
- Prompt response is generated successfully.

**Post-Conditions:**

- GGUF file, associated hash and record of conversion now reside in Google Drive.

**Associated Requirements:**
FR-14
