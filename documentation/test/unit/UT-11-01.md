**Test ID:** UT-11-01
**Test Case Name:** Get Upload Page Displays
**Test Type:** Unit
**Test URL / Location:**

- File: `base_application/tests/test_upload.py`
  **Class:** UploadViewTests
  **Method:** `test_get_upload_page`
  **Context:** Local Execution

```bash
python manage.py test base_application.tests.test_upload.UploadViewTests.test_get_upload_page
```

**Description:**
Verify that the root upload page renders successfully and presents the primary prompt “Submit a Python file”.

**Preconditions:**

- Django test client available.
- Test overrides in place (temporary MEDIA_ROOT, `ALLOWED_HOSTS` includes `testserver`, `SECURE_SSL_REDIRECT=False`).
- Authenticated test user (password-change flag cleared), if the view is protected.

**Execution Steps:**

1. `GET /` (named route: `upload`).
2. Inspect HTTP status and response body.

**Expected Result:**

- Status code **200**.
- Response contains the text **“Submit a Python file”**.

**Post-Conditions:**

- No persistent changes.

**Associated Requirements:**
FR-11
