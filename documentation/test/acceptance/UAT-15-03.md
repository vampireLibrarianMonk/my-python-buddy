**Test ID:** UAT-15-03
**Test Case Name:** Chat Bot ChatSession Model Creation
**Test Type:** User Acceptance
**Context:** Chat Bot Functionality (Model Record Creation)
**Context Ladder** Local then Cloud Deployment

**Description:**
This `User Acceptance` test will validate the functionality of the model record concerning the chat bot code and search functionality.

**Preconditions:**

- Computer instance (with cloud a public IP is provided) is provisioned with running code and is accessible to the User.
- User has been notified that they have to accept self-signed certificate for HTTPS access.
- Environment file configured with valid database URL and secrets.
- User has been given SuperUser credentials.
- User has access to `sample.py` for upload.

## Test Execution Steps

- Note that actual values from table row data will differ (analyzer selection) per User input.

| Step | Action                                                               | Expected Response                                                                                     |
| ---- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| 1    | User uploads `sample.py`                                             | Application accepts file and confirms upload.                                                         |
| 2    | User selects `Analyze`, chooses analyzers and selects `Run analysis` | Analyzer page loads, analysis starts with selected analyzers.                                         |
| 3    | User arrives at `Analyze code` page                                  | Page renders with analysis.                                                                           |
| 4    | User select blue (...) icon.                                         | Chat bot window appears.                                                                              |
| 5    | User enters a question from MAT-13-01 or MAT-13-02                   | Code snippet is successfully generated in the chat window.<br/>Context token tracker updates.         |
| 6    | User enters a question from MAT-13-03                                | Search query summary is successfully generated in the chat window.<br/>Context token tracker updates. |
| 7    | User refreshes page.                                                 | Page resets and chat bot converts to (...) icon.                                                      |
| 8    | User selects blue icon (...)                                         | Chat bot window appears as if it was the first time.                                                  |
| 9    | User selects Admin                                                   | Django Administration Panel appears                                                                   |
| 10   | User selects Runs                                                    | Run history is displayed                                                                              |
| 11   | User navigates to their run entry for `sample.py` and selects it     | Inline Finding and ChatSession history is displayed.                                                  |
| 12   | User navigates back to Admin then selects Chat sessions              | ChatSession history records are displayed.                                                            |

**Post-Conditions:**

- Chat bot session's model fill provenance handled by Django Administration Panel.

**Associated Requirements:**
FR-15
